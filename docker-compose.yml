# Docker Compose file for Autonomous Reasoning Engine

version: '3'
services:
  engine:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - data
    environment:
      - DATA_DIR=/data
  data:
    image: postgres
    volumes:
      - data:/var/lib/postgresql/data
volumes:
  data:
